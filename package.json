{
  "name": "vote-tracking",
  "version": "1.0.0",
  "description": "meta vote tracking stats and DB",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b --verbose",
    "test": "npm run build && node dist/main.js test",
    "dry": "npm run build && node dist/main.js dry test migrate",
    "test-migrate": "npm run build && node dist/main.js test migrate",
    "migrate": "npm run build && node dist/main.js migrate",
    "start": "npm run build && echo 'check scripts/every-hour.sh, include it in crontab'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.20.55",
    "near-api-lite": "github:Narwallets/near-api-lite",
    "pg": "^8.11.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@types/pg": "^8.10.2",
    "typescript": "^5.1.6"
  }
}
