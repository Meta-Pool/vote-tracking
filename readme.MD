This branch (no-sqlite) is cron-running as *hourly_metrics* in the server: validators.narwallets.com
meta-pool-heartbeat bot in the same server reads the json file and publish the data under /metrics
